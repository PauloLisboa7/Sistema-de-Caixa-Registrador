# Frontend

Instruções rápidas:

1. Instalar dependências:

```powershell
cd frontend
npm install
```

2. Rodar em desenvolvimento:

```powershell
npm run dev
```

Por padrão a API é esperada em http://localhost:4000/api. Use `VITE_API_URL` para ajustar.
